
function handleForm(e) {
  e.preventDefault();
  let todoName = document.querySelector("#taskInput").value;

  let personData = {
    id: Date.now(),
    textTodo: todoName,
    isEdit: false,
    isComplete: false,
  };
}

const renderTodoList = () => {

    const maindiv_1 =document.crw

}